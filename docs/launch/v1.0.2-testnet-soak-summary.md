# v1.0.2-testnet — 72h pre-launch soak summary

**Window:** 2026-05-04 02:48 UTC → 2026-05-07 02:48 UTC (72h)
**Fleet:** 5 boxes — 3 seeds (NJ, AMS, Tokyo) + explorer (Dallas) + api (Frankfurt)
**Sample interval:** ~5 min, written locally to `/var/lib/coincync/soak/<start>.jsonl` per box
**Generator:** `/usr/local/bin/coincync-soak` (oneshot systemd unit)

## Headline result

> *(fill on 2026-05-07 02:48 UTC after soak completes)*

## Per-box metrics — final

> Run `scripts/soak-final-summary.sh` after the soak finishes and paste the output here.

| Box | Samples | Max stall | Desync samples | Final tip | Notes |
|---|---|---|---|---|---|
| seed1 (NJ) | TBD | TBD | TBD | TBD | |
| seed2 (AMS) | TBD | TBD | TBD | TBD | |
| seed3 (Tokyo) | TBD | TBD | TBD | TBD | |
| explorer (Dallas) | TBD | TBD | TBD | TBD | |
| api (Frankfurt) | TBD | TBD | TBD | TBD | |

**Tip convergence:** *(yes/no — all boxes within ±1 of the same height)*

## Known incidents

### 1. Explorer peer-wedge (2026-05-05) — RESOLVED

**Pathology:** explorer's `coincync-node` selected one peer for IBD, sent `GetBlocks`, peer responded with `Blocks` message containing 0 entries, request timed out, requeued. Same peer stayed selected, loop forever. Different IPs each occurrence; identical behavior. Two recurrences in 36h on the curated fleet (15.1.193.74 on 2026-05-04 / 5.4h, 75.82.181.240 on 2026-05-05 / 13h) confirmed it was not a fluke.

**Soak signal:** `stall_max = 46809s` (13h) on the explorer box, captured before the fix landed. Other 4 boxes max-stall ~40-45 min (normal block-time variance for a low-hashrate testnet).

**Fix:** [commit 28b3420](https://github.com/CyncDevelopment/Cync-Protocol/commit/28b3420) — per-peer consecutive-empty-Blocks counter in `src/network/scoring.rs`. After 5 consecutive empties, peer is demoted in scoring and banned from `GetBlocks` selection for 1h. Auto-expires.

**Deployed:** mid-soak across all 5 fleet boxes. Post-deploy samples should show steady-state stalls ≤ 5 min.

**Verification:** 3 unit tests in `src/network/scoring.rs` pass; live fleet has not exhibited a new peer-wedge since the deploy.

### 2. *(add any new incident discovered in the final 12h here)*

## Decision

> *(fill 2026-05-07: "GO for week-of-2026-05-09 announcement" / "HOLD, need …" / etc.)*

## Reproducing this report

```bash
# After soak completes, from the repo root:
./scripts/soak-final-summary.sh

# Output is the markdown table you paste into the "Per-box metrics" section above.
# Source files live on each box at /var/lib/coincync/soak/<startTimestamp>.jsonl
```

## What's next

- [ ] If GO: post `docs/launch/TESTNET_LAUNCH_ANNOUNCEMENT.md` to Discord, BitcoinTalk ANN, r/CryptoCurrency, HN
- [ ] If HOLD: open issue with the failing metric + fix plan + re-soak window
- [ ] Either way: archive the 5 `.jsonl` source files into `release/v1.0.2-testnet/soak/` for posterity
