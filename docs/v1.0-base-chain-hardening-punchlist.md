




<!-- markdownlint-disable MD012 MD013 MD032 MD036 MD040 MD060 -->
# v1.0 Base-Chain Hardening Punch List

> **RESOLVED 2026-05-23 (evening):** SIMD / Rust-toolchain blocker closed. `rust-toolchain.toml` pinning workspace to **1.88.0** landed in `a7f0a6d` and was empirically verified: full `cargo clean && cargo build --release` of the workspace completed in 9m 42s with 0 errors, 0 warnings. The SIMD overflow that affected Rust 1.92 does not manifest on 1.88. The Dockerfile's existing `ARG RUST_VERSION=1.88.0` matches; Docker reproducible-build verification in progress.
>
> Resolution note: the prior recommendation to pin "1.85 or 1.86" was over-cautious — empirically 1.88.0 is the correct floor (matches the Dockerfile pin), and the SIMD error only manifests on 1.92+. Owner: closed.
>
> Original investigation entries preserved below for context.
>
> ---
>
> **UPDATED 2026-05-23 (later):** the SIMD/Rust-toolchain blocker is now bigger than originally documented. Investigation summary:
>
> - Local toolchain rolled to **Rust 1.92.0** during the session. With the cached `target/` artifacts the build still appears clean, but a fresh `cargo clean && cargo build --release` reproduces the `Simd<_, _>: Add` overflow error in `src/crypto/bulletproofs.rs` (lines 470, 534, 576, 631), not just in the `tari_bulletproofs_plus` dep — meaning the issue now affects production builds, not just Docker repro.
> - Attempted fix: bump `tari_bulletproofs_plus` from `"0.4"` → `"0.5"`. Result: SIMD error disappears, but a different error surfaces in `utoipa-swagger-ui = "9.0.2"` (`error[E0599]: no function or associated item named 'get' found for struct 'SwaggerUiDist'`). 9.0.2 is the latest published version. Cargo.toml has been reverted to `0.4`.
> - Cleanest path forward: pin Rust via a `rust-toolchain.toml` to a known-good version (probably 1.85 or 1.86 based on the original Dockerfile floor reasoning). Requires identifying the right version + verifying the workspace still resolves with `time 0.3.47+` and `cpufeatures 0.3.0`. Multi-build investigation, ~1-2 hours focused.
> - **Until resolved, ship from cached artifacts** — the v1.0.9-testnet-pre-audit binaries already on disk were built before the 1.92 rollup and remain valid. New release tags built on Rust 1.92 will fail until the toolchain pin lands.
>
> **Original 2026-05-23 entry below for history:**
>
> **OPEN follow-up 2026-05-23: reproducible Docker build blocked on Rust-1.88 SIMD compat.**
>
> The reproducible-build verification (Dockerfile at `docker/builder.Dockerfile`, wrapper at `scripts/build-in-docker.sh`) surfaced two missing-file audit-blockers — **already fixed in `3a16bed` (orchard-side) and `fe7ed6b` (src/network/socks_dns.rs)** — plus a third remaining blocker that's NOT a missing file:
>
> ```text
> error[E0275]: overflow evaluating the requirement `for<'v> &'v Simd<_, _>: Add`
> --> tari_bulletproofs_plus::RangeProof<P>
> ```
>
> Cause: `tari_bulletproofs_plus = "0.4"` uses `std::simd` in a way that errors on Rust 1.88+'s stricter higher-rank-trait-bound checking. Local builds work because the host toolchain is < 1.88; the Docker image pins 1.88.0 (required by `time 0.3.47+` and `cpufeatures 0.3.0` per Dockerfile comment).
>
> **Resolution paths** (any one suffices, all are ~30-60 min focused investigation):
>
> 1. Check if `tari_bulletproofs_plus 0.5+` exists with the SIMD compat fixed.
> 2. Pin to `tari_bulletproofs_plus = "=0.4.0"` (or older 0.3.x) if there's a known-working revision.
> 3. Patch via `[patch.crates-io]` in workspace `Cargo.toml` against a known-good fork.
> 4. Pin Docker Rust to ≤ 1.87.0 AND down-pin `time` + `cpufeatures` to versions that don't require edition2024. Cascading dep work.
>
> **NOT a v1.0 ship blocker** in the strict sense — the chain builds and runs locally + on CI. The reproducible-build *claim* in `docs/operations/REPRODUCIBLE_BUILDS.md` is held back until this is resolved. Audit firm will care about it but it's separable from the consensus-code audit.
>
> Owner: TBD post-mainnet polish. Tracked in `docs/operations/REPRODUCIBLE_BUILDS.md` status table.


**Generated:** 2026-05-21
**Source:** 10 parallel scan agents covering ~165 .rs files across the v1.0 audit perimeter (`src/` + v1.0 supporting crates: FROST coord, rolling-finality, mining rig). Cyncswap, Orchard, CyncHub, and wallet UI are excluded — they're not v1.0.

**Purpose:** close low-hanging audit findings ourselves before the audit firm starts. Every item below has a `file:line` reference. Every H-severity item should be **read in source before fixing** — prior subagent work on this codebase has had ~50% false-positive rate on file:line specifics; treat this doc as a list of *places to look*, not as a list of confirmed bugs.

**Severity:**

- **H** — exploitable today, consensus-divergence risk, panic in receive path, key leak, or auth bypass
- **M** — likely audit finding, quality-of-implementation gap, DoS-enabling, undocumented invariant
- **L** — polish, defensive coding, documentation

---

## Top 10 highest-impact items (recommended first pass)

These are the items most likely to be real, most likely to be quick fixes, and most likely to cleanly reduce the audit-finding count. Read the source first, fix, then move down the list.

| # | File:Line | Issue | Why first |
|---|---|---|---|
| 1 | ~~[src/consensus/rolling_finality.rs:178,208,219,228](c:\dev\coincync\src\consensus\rolling_finality.rs)~~ **DONE 2026-05-21** | ~~4× `RwLock::read().expect("lock poisoned")` — any thread panic mid-lock crashes the node~~ | ~~Replace with `parking_lot::RwLock`~~ Swapped `std::sync::RwLock` → `parking_lot::RwLock`, removed 4 `.expect()` calls. 8/8 module tests pass under `--features rolling-finality`. |
| 2 | ~~[src/rpc/explorer.rs:1068-1154]~~ **CORRECTED to [src/rpc/rest.rs:1057-1154](c:\dev\coincync\src\rpc\rest.rs)** **DONE 2026-05-21** | ~~WebSocket handler has no per-connection idle timeout~~ — confirmed real after correcting file path | Added `WS_IDLE_TIMEOUT = 300s` + `idle_check` interval arm to the `tokio::select!`; also now treats `Some(Err(_))` as close (was silently ignored). Compiles clean. |
| 3 | ~~[src/rpc/explorer.rs:1137-1152]~~ **FALSE POSITIVE — STRUCK** | ~~`socket.send().await` not error-checked~~ | Every `socket.send()` is already wrapped in `.is_err() { break }` at lines 1097, 1112, 1130, 1143 of rest.rs. Backpressure-induced send failure DOES close the connection. Agent misread the code. |
| 4 | ~~[crates/coincync-rig/src/metrics.rs:228](c:\dev\coincync\crates\coincync-rig\src\metrics.rs)~~ **DONE 2026-05-21** | ~~Metrics endpoint defaults to `0.0.0.0`~~ | `serve()` now takes a `bind_addr`; new `--metrics-bind` CLI flag + `metrics_bind` TOML field, default `127.0.0.1`. Loud `tracing::warn!` when bind is `0.0.0.0` / `::`. Operators upgrading must set `--metrics-bind 0.0.0.0` to restore prior behavior. |
| 5 | ~~[src/network/noise.rs:478](c:\dev\coincync\src\network\noise.rs)~~ **DONE 2026-05-21** | ~~Noise handshake frame allows up to 65 KB~~ — confirmed: existing `if len > 65535` was dead code (u16 max). | Added `NOISE_HANDSHAKE_FRAME_MAX = 8192` const; frame size check enforces it with diagnostic error. 3/3 noise tests pass. DoS surface dropped ~64 KB → 8 KB per handshake attempt. |
| 6 | ~~[src/db/blocks.rs:157](c:\dev\coincync\src\db\blocks.rs)~~ **DONE 2026-05-21** | ~~`unwrap_or([0u8; 8])` on height key parse~~ — confirmed | Replaced silent default with `DatabaseError` carrying the actual malformed key length. Caller `tip()` propagates correctly. 1/1 block-storage test passes. |
| 7 | ~~[src/bin/node.rs:128](c:\dev\coincync\src\bin\node.rs)~~ **DONE 2026-05-21** | ~~Tracing init panics if RUST_LOG malformed~~ — confirmed | Chained fallback: try RUST_LOG → try `--log-level` arg → fall back to hardcoded `"info"` (guaranteed-valid). Warns to stderr if the operator's filter was invalid. |
| 8 | ~~[src/rpc/explorer.rs:411-447]~~ **CORRECTED to [src/rpc/rest.rs:411-426](c:\dev\coincync\src\rpc\rest.rs)** **DONE 2026-05-21** | ~~Rate limiter trusts X-Forwarded-For~~ — confirmed real | XFF/XRI now gated behind `COINCYNC_TRUST_PROXY_HEADERS=1` env var (cached via `OnceLock`). Default: untrusted → all callers share "unknown" bucket = effective global rate limit (safer than spoofable per-IP). Deployments behind nginx/Caddy opt in via the env var. |
| 9 | ~~[src/rpc/server.rs:1039-1054]~~ **CORRECTED to [src/rpc/rest.rs:1055-1110](c:\dev\coincync\src\rpc\rest.rs)** **DONE 2026-05-21** | ~~Global 128 WS cap, no per-IP cap~~ — confirmed | Added `MAX_WS_PER_IP = 5` const + `ACTIVE_WS_PER_IP` HashMap counter behind `parking_lot::Mutex`. Per-IP check happens BEFORE global CAS dance; over-cap returns `429 Too Many Requests`. `WsConnGuard` now decrements both counters on drop. Collapses to global cap when XFF untrusted (#8) — fine because the global cap was the only protection before. |
| 10 | ~~[src/rpc/lightwallet.rs:201-203](c:\dev\coincync\src\rpc\lightwallet.rs)~~ **DONE 2026-05-21** | ~~`max_blocks` silently clamped to 5000~~ — confirmed | Added explicit `MAX_SCAN_BLOCKS_PER_REQ = 5000` const; over-cap now returns `Err("max_blocks too large: N (limit 5000); paginate by advancing start_height")`. Docstring on `ScanRequest::max_blocks` updated. 3/3 lightwallet tests pass. |

If you do nothing else on this list before audit, do these 10.

---

## Network layer — `src/network/` (14 findings)

```
✓  src/network/noise.rs:478              Handshake frame size cap 65535 — should be ~8KB         ALREADY FIXED in HEAD (verified 2026-05-24): NOISE_HANDSHAKE_FRAME_MAX = 8192 at line 42, enforced by read_noise_frame at lines 529-534 before vec![0u8; len] alloc. Punchlist entry was stale (the cited line 478 is the SEND side which uses the full u16::MAX for established-session frames; that's the right cap for that path).
✓  src/network/socks_dns.rs:202          DNS-over-TCP zero-length resp handling asymmetric        FIXED 2026-05-24: symmetric bounds check added on outbound query (lines ~181-194); request side now rejects empty + >MAX_DNS_RESPONSE-byte queries the same way the response side does
M  src/network/bootstrap.rs:352-362      Tried-set eviction order is adversary-influenced         Bias eviction by recency/LRU. STILL OPEN (verified 2026-05-24): current code uses `self.tried.iter().next()` for eviction which is HashSet iteration order ≈ hash-derived ≈ adversary-influenceable in principle. M-severity but the fix requires a real data-structure swap (HashSet → IndexMap with explicit timestamp/recency tracking, or LRU-cache crate). Defensible to defer until after the audit firm flags it specifically. MAX_TRIED bound of 5000 already prevents the worst case (unbounded memory growth + permanent outbound isolation).
M→DOC src/network/framing.rs:126        64KB initial buf vs 16MB max → vec doubling thrash      DOC ADDED 2026-05-24: expanded the SECURITY comment to spell out the PERF TRADE-OFF — worst-case 16 MB payload triggers ~8 reallocs (Vec doubles 64K → 128K → … → 16M) and a final ~8 MB memcpy on the last grow. Per-connection cost; pooled-buffer reuse would amortize if throughput becomes a measured bottleneck. Currently accepted because memory-exhaustion safety dominates on a fresh peer (attacker claims 16M and never sends → we commit 64K not 16M). Code unchanged; tradeoff now explicit for the audit firm.
~~M  src/network/protocol.rs:614,621~~    **DONE 2026-05-21.** Same pattern as Argon2 — `expect()` retained, message tightened to spell out the "single u64, infallible" invariant + pointer to caller sites if PingPongMessage layout ever grows.
M→DOC src/network/peer.rs:255-260       64KB read buf + 10MB/s rate-limit + per-byte read loop  DOC ADDED 2026-05-24: expanded the SECURITY doc on `PeerConnection::run` to spell out the 1-byte-frame loop-iteration cost. `reader.read()` returns as soon as ≥1 byte is available, so an adversary sending 1-byte TCP frames forces ~10M loop iterations/sec per connection (bounded by MAX_BYTES_PER_SECOND). Mitigation (switch to `read_exact(12)` for the frame-header prefix) deferred until profiled — rate limits already cap per-connection CPU cost. The punchlist's "300s timeout" reference appears to be stale; no such constant exists in this file (HEAD has only the per-second rate check + 60s rolling window + WINDOW_BYTE_LIMIT). Doc explains the actual trade-off the audit firm should see.
✓  src/network/connection_tracker.rs:78  IPv4 /16 vs IPv6 /16 — link-local vs global undocumented FIXED 2026-05-24 (commit 473b9fb): expanded the subnet_key() doc to explain the IPv4 vs IPv6 /16 asymmetry (IPv4 /16 ≈ ISP block; IPv6 /16 ≈ RIR allocation — same numeric width = more aggressive grouping for IPv6, intentional because IPv6 attackers can trivially mint addresses from a single /64). Link-local IPv6 handling also explained.
~~M  src/network/dns_seeds.rs:131-134~~  **DONE 2026-05-21.** Per-entry parse failures now log a `tracing::warn!` with the malformed string and error — operator sees the issue instead of getting silent empty seed list.
L  src/network/socks_dns.rs:241-252      DNS tx ID = nanos XOR 0xC0FE → sub-ms collisions         Use OsRng for 16-bit ID
L  src/network/bootstrap.rs:95-102       TESTNET_P2P_PORT hardcoded at call site                  Move to BootstrapConfig
L  src/network/noise.rs:44               icacls failure silently ignored on Windows               Log + return Result
L  src/network/framing.rs:231-238        Hex-dump of invalid magic at warn level → log spam        Move to debug or sample
L  src/network/bootstrap.rs:606-626      peer.json deserialize with no pre-size check             Reject files > 10 MB
L  src/network/node.rs:1019-1023         Arc-wrapped subnet slot in spawn — pattern unclear       Add comment or simplify
```

**Network verdict:** no critical consensus-level bugs found. Noise XX handshake (via `snow` crate), SOCKS5 auth, eclipse-tracking, /16 limits, Dandelion randomization, per-type message-size limits, framing checksum, and reputation decay all looked solid.

---

## Consensus — `src/consensus/` (26 findings, several need verification)

```
✓  src/consensus/rolling_finality.rs:178,208,219,228   RwLock poison → node crash               ALREADY FIXED in HEAD (verified 2026-05-24): line 61 imports `parking_lot::RwLock` (panic-free); FinalityTracker singleton wraps it. No std::sync::RwLock in this module. Stale punchlist entry.
~~H? src/consensus/validation.rs:197~~       FALSE POSITIVE — `<=` is correct (blocks AT-OR-BELOW the checkpoint are pre-verified by that checkpoint)
~~H? src/consensus/validation.rs:749~~       Demoted to L — u64::MAX overflow unreachable (~70T years at 2-min blocks)
~~H? src/consensus/validation.rs:783~~       FALSE POSITIVE — `>=` correctly rejects votes for own-or-future heights; voting for self is nonsensical
~~H  src/consensus/validation.rs:846~~       **DONE 2026-05-21** — replaced bytewise comparison with `target_to_u128` for consistency with line 862. `critical_files.lock` re-hashed (`3fc83f79… → b531e653…`). Build accepts new hash; 3/3 validation tests pass.
~~H? src/consensus/validation.rs:852~~       Demoted to L — zero-target check is a valid defensive guard; "compare against min_target" is cosmetic
✓  src/consensus/difficulty.rs:91        Empty/single-block ASERT undefined                       ALREADY FIXED in HEAD (verified 2026-05-24): `if blocks.len() < 2 { return max_target(); }` at line 97 is the explicit ≥2 guard. Stale entry.
✓  src/consensus/difficulty.rs:103-104  saturating clamps on diff bounds                          ALREADY DOCUMENTED in HEAD (verified 2026-05-24): the comment block at lines ~104-108 explains "ASERT below the unsafe range where production rate outruns P2P propagation" and references MIN_DIFFICULTY. Floor justification is in-code.
✓  src/consensus/difficulty.rs:109       MIN_DIFFICULTY division — if 0, panic                    FIXED 2026-05-24 (commit aa23134): added `const _: () = assert!(MIN_DIFFICULTY > 0, ...)` compile-time assertion. The literal value (500) is obviously nonzero, but the const_assert locks the invariant for any future change.
N/A src/consensus/difficulty.rs:142-143  checked_mul.unwrap_or(i128::MAX) silently clamps         NOT-A-BUG (verified 2026-05-24): the code is `time_error.checked_mul(RADIX as i128).unwrap_or(if time_error >= 0 { i128::MAX } else { i128::MIN })` — DIRECTIONAL saturation (MAX for positive overflow, MIN for negative). Not a silent clamp; correct saturation semantics for the ASERT exponent calc. Returning Err would force every difficulty call site to handle a "shouldn't happen" error path; not worth the refactor cost.
✓  src/consensus/difficulty.rs:156       (-x as u32) on neg int — large shift                     FIXED 2026-05-24: added a SAFETY comment block explaining the bounded cast — `clamped_int` is bounded to [-MAX_INT_EXPONENT, MAX_INT_EXPONENT] by the prior `.clamp(...)`, so the negation + `as u32` cast can't wrap. The `>= 128` guard then caps the shift before it could exceed u128 width.
✓  src/consensus/fork_signal.rs:160-161 ratio can exceed 100% when total < window                FIXED 2026-05-24: changed `let pct = (count * 100 / total) as u32` to `let pct = ((count * 100 / total) as u32).min(100)`. Defense against the edge case where `total` is `.max(1)`-clamped at the start of a window but count is non-zero — UI no longer sees nonsensical "500% signaling" during window transitions. INVARIANT comment block added explaining the rationale.
~~M  src/consensus/validation.rs:352,608~~  Congestion u128 precision at boundaries                  **VERIFIED + FOLLOW-UP FLAGGED 2026-05-24:** boundary tests already exist for the wallet-side `congestion_multiplier()` in fee_market.rs (test_congestion_multiplier at lines 296-305 covers 0, 49, 50, 74, 75, 89, 90, 100 exhaustively). However the validator inlines a parallel `multiplier_x10` table at validation.rs:609-617 — identical thresholds + ratios (10/15/20/30 vs fee_market's 100/150/200/300, mathematically equivalent for all q where `q * multiplier` doesn't approach u64::MAX). A divergent edit to one without the other is a consensus-fork surface. **Refactor planned for a dedicated commit:** delegate validator to `fee_market::congestion_multiplier` (bit-identical, but touches a critical_files.lock file — needs full test-suite run + lockfile hash refresh + careful audit-firm-visible justification in the commit message). Boundary-precision concern itself is N/A — u128 intermediate has plenty of headroom (size ≤ MAX_BLOCK_SIZE ≪ u64::MAX/100).
✓  src/consensus/validation.rs:817       Timestamp drift uses > not >=                            FIXED 2026-05-24 (documentation): added SPEC comment block explaining the bound is INCLUSIVE (matches Bitcoin Core `nMaxFutureBlockTime` + NTP/RFC 5905 convention). Switching to `>=` would reject legitimate boundary-equal timestamps with no security gain. Code unchanged — already correct; documentation made unmissable.
L  src/consensus/block.rs:52-59          Empty-block merkle root = Hash::zero                     Verify matches genesis
L  src/consensus/fee_market.rs:31-32     saturating_mul order                                     Add edge-case test
L  src/consensus/privacy_policy.rs:75,82 RistrettoPoint decompress / identity check               Verify against curve25519_dalek
L  src/consensus/validation.rs:356-375   Fee distribution u128 intermediate                       OK; document
L  src/consensus/validation.rs:517       Coinbase sum overflow loop break                         Verify idx in error msg
```

**Consensus verdict:** The 4× `RwLock` poison expect() in [rolling_finality.rs](c:\dev\coincync\src\consensus\rolling_finality.rs) is a real, fixable bug — swap to `parking_lot::RwLock` and the H-severity items collapse to verify-each-then-fix. Everything else under validation.rs needs source-read first; the agent flagged many boundary-condition concerns but several may be intentional.

---

## Crypto primitives — `src/crypto/` (11 findings, mostly polish)

```
✓  src/crypto/clsag.rs:331               Add explicit doc on canonical scalar rejection          FIXED 2026-05-24: SECURITY comment block added above the responses filter_map explaining the malleability defense (filter_map drops non-canonical scalars + len() != n check rejects the whole signature)
✓  src/crypto/bulletproofs.rs:305-306   to_bytes() uses expect() on borsh serialize             ALREADY FIXED in HEAD (verified 2026-05-24): `try_to_bytes()` at line 310 returns Result. `to_bytes()` retains expect() intentionally per the SECURITY comment ("Never silently return empty data on serialization failure") for callers that prefer panic over corrupt empty output. Both call shapes available.
N/A src/crypto/lelantus_spark.rs:265     thread_rng() in build_anon_set — VERIFY consensus path  NOT-A-BUG (verified 2026-05-24): build_anon_set is prover-side (SparkAccumulator helper). The verifier validates the proof against the encoded indices; the prover's randomness in choosing decoys is the whole point of the privacy mechanism. Determinism here would defeat anonymity. Not a consensus path.
N/A src/crypto/ring_selection.rs:265     thread_rng() in Fisher-Yates shuffle — likely wallet     NOT-A-BUG (verified 2026-05-24): the `rng` is a function parameter passed by the caller (wallet-side `select_decoys`). Random decoy selection IS the privacy mechanism — predictable selection enables transaction graph analysis. Wallet-side, prover-time, correctly random.
N/A src/crypto/bulletproofs.rs:459       from_bytes_mod_order on encoded scalars                  NOT-A-BUG (verified 2026-05-24): the `from_bytes_mod_order` calls in bulletproofs.rs (lines 76, 84, 125) are on BlindingFactor (a secret, wallet-side scalar). Blinding factors are never wire-deserialized — they're stored in the wallet. The wire-deserialized scalars in CLSAG use `from_canonical_bytes` (with malleability protection — see clsag.rs:331 SECURITY comment shipped in df6bac7).
✓  src/crypto/batch_verify.rs:179       unwrap_or(false) on HashMap lookup — add safety comment  FIXED 2026-05-24 (commit aa23134): added a SECURITY comment block explaining that `unwrap_or(false)` is the conservative default (no-result-in-batch ⇒ reject rather than silently accept), and documenting the caller invariant.
L  src/crypto/curve.rs:83                from_bytes uses mod-order reduce                        Add docstring distinguishing use
L  src/crypto/view_keys.rs:18-19         #[zeroize(skip)] semantics not obvious                  Add docstring on Drop behavior
L  src/crypto/disclosure.rs:148          Scalar::from_bytes_mod_order_wide on 512-bit hash       OK; document why no bias
L  src/crypto/stealth.rs:814             tracing::trace! of output_idx                           No risk; trace only
L  src/crypto/mw_cutthrough.rs:240       Compress-then-compare point equality                    OK (Ristretto canonical)
```

**Crypto verdict:** Architecturally solid. The cited issues are mostly documentation gaps and a few `expect()` calls in code labeled "should never fail." Constant-time discipline, zeroization, and subgroup checks all looked correct. **However:** `lelantus_spark.rs:265` needs verification — if `build_anon_set` is reachable from a consensus codepath, `thread_rng()` is a real consensus-divergence bug; if it's wallet-side ring decoy selection, it's fine.

---

## Mining + rig — `src/mining/` + `crates/coincync-rig/` (16 findings)

```
✓  crates/coincync-rig/src/metrics.rs:228       Metrics binds 0.0.0.0 by default                ALREADY FIXED in HEAD (verified 2026-05-24): default is 127.0.0.1 at the CLI/config layer (per comment); `serve()` warns via tracing::warn! when bound to 0.0.0.0 / :: with operator guidance ("firewall the port or bind to 127.0.0.1 / a specific internal IP"). Stale punchlist entry.
✓  src/mining/stratum.rs:87                     Stratum accepts public bind w/o TLS req          ALREADY FIXED in HEAD (verified 2026-05-24): default bind is `127.0.0.1:3333` (loopback); `validate_stratum_exposure_policy()` rejects public bind unless 3 conditions met (COINCYNC_STRATUM_PUBLIC_BIND_ACK=1, password set via COINCYNC_STRATUM_PASSWORD, AND (TLS native OR COINCYNC_STRATUM_TLS_PROXY_ACK=1)). Stale.
✓  src/mining/stratum.rs:123-128                Public Stratum + tls_enabled=false → no enforce  ALREADY FIXED in HEAD: same validate_stratum_exposure_policy() — explicit `tls_enabled || COINCYNC_STRATUM_TLS_PROXY_ACK` gate. Stale.
✓  crates/coincync-rig/src/daemon.rs:105-112    Bearer-token header has no char validation       ALREADY FIXED in HEAD (verified 2026-05-24): line 133 uses `HeaderValue::from_str(&bearer).context("api key contains illegal characters for an HTTP header")?` — the from_str validator rejects control chars and `?` propagates the error to the caller. Stale.
DEFER-OP src/mining/pool.rs:487-493            PPLNS share pruning advisory not hard cap        DEFENSIBLE DEFER (verified 2026-05-24): prune runs at the END of `calculate_payouts` (lines 487-493); shares stay bounded around 2× pplns_window IF payouts are called regularly (per-block typical). Operator-visible failure mode: if payouts pause, memory grows linearly with share-submit rate. A hard `MAX_PENDING_SHARES = 100k` cap would silently drop shares (worse for miners — they don't get credited). Current behavior fails loudly via OOM rather than silently via dropped credit. Acceptable for an off-by-default pool component.
✓  crates/coincync-rig/src/orchestrator.rs:313  u64::MAX / n_threads — n=0 panic guard          ALREADY FIXED in HEAD (verified 2026-05-24 in df6bac7's sweep): line 308 has `let n_threads = threads.max(1);` BEFORE the `u64::MAX / n_threads as u64` divide, so n_threads is always ≥1. Stale entry.
✓  src/mining/stratum.rs:570                    Worker job channel unbounded                     ALREADY BOUNDED in HEAD (verified 2026-05-24): line 570 uses `mpsc::channel::<String>(32)` (tokio's bounded constructor — capacity 32, even tighter than the punchlist's suggested 128). Punchlist entry was a misread; tokio's `mpsc::channel(N)` is bounded, `mpsc::unbounded_channel()` is the unbounded one.
✓  src/mining/template.rs:70-95                 Block template includes mempool txs w/o re-val   ALREADY FIXED in HEAD (verified 2026-05-24): lines 83-95 re-validate every candidate tx via `chain.validate_transaction(tx)` and skip chain-invalid ones with a tracing::debug! log. Doc comment at lines 72-82 cites the historical "poison-template" incident (2026-05-08 duplicate-key-image) this fix addressed. Punchlist entry was stale — the belt-and-suspenders miner-side filter is in place; the matching admission-side shadow-evict on chain.set_height is still flagged as TBD in the source comment.
DEFER-CONFIG crates/coincync-rig/src/stratum.rs:286-292    30s response timeout hardcoded         INTENTIONAL DEFAULT (verified 2026-05-24): the 30s stratum-response timeout has an in-code doc ("generous for pool round-trip; if the pool stalls longer than that, treat the connection as dead"). Making it configurable means a daemon field + CLI flag + tests for what is effectively a sensible-for-every-pool default. The current value is fine for any reasonable pool (typical RTT << 1s; 30s catches truly broken pools without false positives). Defer until an operator reports a real pool that needs >30s.
DEFER-AUTH src/mining/stratum.rs:840-855       Per-job duplicate-share set unbounded            DEFENSIBLE DEFER (verified 2026-05-24): `w.submitted_shares.clear()` fires on every new job_id (line 847), so the set is bounded by submit-rate-during-one-job (typically <30s job lifetime). An adversarial miner could rapid-fire shares against a single job to grow the set; the mitigation is workers are auth-gated (config.auth_password, line 562) so the attacker must first compromise a worker password. Bounded-LRU dedup would harden a layer that's currently protected by upstream auth; lower audit priority than no-auth-required DoS surfaces.
✓  src/mining/pool.rs:906-911                   Difficulty saturating_mul silently clamps to 0   FIXED 2026-05-24: line 910 (decrease path) now uses `saturating_mul(7) / 10` (was raw `* 7 / 10` — panic in debug, wrap in release for difficulty > u64::MAX/7 ≈ 2.6e18). Line 906 (increase) already used saturating_mul correctly. SAFETY comment added explaining why saturate-then-divide is required (the subsequent `.max(min_difficulty)` catches wrap-to-near-zero but NOT wrap-to-medium).
L  crates/coincync-rig/src/daemon.rs:61-70      http:// URLs accepted for remote node             Warn when remote + http
L  crates/coincync-rig/src/worker.rs:85         NonceRange split saturating                       Clarify comment
L  src/mining/stratum.rs:1168-1188              Tests use expect() — model                       Test-only, low priority
L  crates/coincync-rig/src/main.rs:303-306      Thread auto-detect falls back to 1 silently      Log fallback reason
L  src/mining/pool.rs:451-463                   Fee calc uses float intermediate                  Document precision loss
```

**Mining verdict:** Two real findings worth fixing today: **metrics 0.0.0.0** and **Stratum public-bind-without-TLS**. Both are operator-deployable footguns rather than node-critical, but auditors will write them up.

---

## RPC — `src/rpc/` (10 findings, mostly DoS hardening)

```
⚠  src/rpc/explorer.rs:1068-1154   WebSocket: no idle timeout                              PHANTOM line reference (verified 2026-05-24): src/rpc/explorer.rs is only 185 lines long. Either the line refs were against a different file/version, or substantial code was removed since punchlist generation. Re-scan with current code before re-flagging.
⚠  src/rpc/explorer.rs:1137-1152   WebSocket: socket.send() unchecked                       PHANTOM line reference: same as above.
M  src/rpc/explorer.rs:411-447     X-Forwarded-For trusted by rate limiter                  Whitelist reverse-proxy IPs
M  src/rpc/server.rs:1039-1054     128 WS cap is global, no per-IP                          per_ip ≤ 5
✓  src/rpc/lightwallet.rs:201-203  max_blocks silently clamped 5000                          ALREADY FIXED in HEAD (verified 2026-05-24): code at lines 211-217 explicitly rejects with error `"max_blocks too large: {} (limit {}); paginate by advancing start_height"`. Docstring at lines 103-105 also documents the rejection. Stale entry.
✓  src/rpc/lightwallet.rs:39      MAX_SCAN_OUTPUTS=10k undocumented + scanned_to_height bug FIXED 2026-05-24: (1) constant doc now spells out the API contract — hitting the cap is NOT an error, scan returns early with `has_more = true` + partial outputs, caller advances `start_height` past the highest received `output.height` to continue. (2) `ScanResponse::has_more` doc now distinguishes the two reasons it can be set (range didn't reach tip vs cap fired mid-block) and tells callers to resume at `scanned_to_height` (not `+1`) when capped. (3) Real bug fixed: when cap fired mid-block at height `h`, `scanned_to_height` was reported as `end_height` (the requested range end) — a caller resuming at `end_height + 1` would silently skip blocks `h+1..=end_height`. Now reports `last_h` (the actual cap-block) when capped, `end_height` otherwise.
L  src/rpc/server.rs:266-267       Bearer token length-checked first                        OK (timing leak mitigated)
L  src/rpc/server.rs:281,302       expect()/unwrap_or_default on response build             OK (unreachable)
L  src/rpc/rest.rs:228-234         X-Forwarded-For trust implicit                            Whitelist required
L  src/rpc/types.rs:64             AddressInfo.balance field present                        Type-only, never returned
```

**RPC verdict:** Authentication is constant-time (good). Article IX privacy compliance is intact (no balance-by-address endpoints). The two H items are both DoS-class on the explorer WebSocket — easy fixes that close the most likely audit finding.

---

## Wallet library — `src/wallet/` (21 findings)

```
~~H  src/wallet/key_epoch.rs:80-90~~     **FALSE POSITIVE — STRUCK 2026-05-21.** `ScopedViewKey::to_json()` is the deliberate selective-disclosure export.
~~H  src/wallet/lightsync.rs:452~~       **FALSE POSITIVE — STRUCK 2026-05-21.** Both sequential + parallel scan paths check subaddress keys identically (`C9-FIX`).
H  src/wallet/persistence.rs:501        **DEFERRED — unscheduled future-work.** NOT slotted for v1.0, v1.0.x point releases, or v1.1 (cyncswap-only). Header KDF params not HMAC-authed. Design conversation opened 2026-05-21 at [docs/security/wallet-file-v4-design.md](security/wallet-file-v4-design.md) — Option A (HMAC over full file with HKDF-derived independent MAC key) selected. Implementation cost: ~1-2 focused days. Not yet scheduled.
~~H→L src/wallet/persistence.rs:503-548~~ **Demoted to L 2026-05-21.** Both write-fail and rename-fail paths DO `remove_file`. Temp file is always ciphertext, not plaintext. Concern is contrived.
~~H  src/wallet/persistence.rs:372~~     **DONE 2026-05-21.** expect()s retained (14-caller breaking change disproportionate for OOM defense), but messages now spell out the `KdfParams::validate()` invariant for future refactors.
~~H  src/wallet/persistence.rs:189-247 (validate cross-constraint)~~ **DONE 2026-05-23 (commit 6e06b6f).** Closed a panic-on-malformed-input DoS surfaced by fuzz overnight #4 (crash 1c3a7e3e...). validate() individually bounded m_cost, t_cost, p_cost but missed the RFC 9106 §3.1 cross-constraint `m_cost ≥ 8 × p_cost`. A crafted header with `m_cost=8 + p_cost=16` passed validate(), reached `Argon2::Params::new()` which panicked with `MemoryTooLittle`. Added the cross-constraint check before `Ok(())`. 17/17 wallet::persistence tests still pass.
~~M  src/wallet/persistence.rs:218-220 (KDF upper caps)~~ **DONE 2026-05-23 (commit 69c27dc).** Tightened validate()'s upper bounds from 1 GiB / 100 iter / 16 lanes to 512 MiB / 32 iter / 8 lanes. Closes 3 slow-units (025434bd, 0a9d8f66, 7487547a) where Argon2 spent 30+ sec on a single legit-but-pathological wallet input. Defaults (m=256 MiB, t=3, p=4) sit well below new caps; no legit-wallet impact.
~~H/M src/wallet/keys.rs:79~~            **FALSE POSITIVE — STRUCK 2026-05-21.** Blake2b `personal` is domain separation; per-FVK uniqueness comes from `rivk`. Not identity binding.
✓  src/wallet/multisig.rs:347-418       reconstruct_group_secret returns raw [u8;32]               FIXED 2026-05-24: signature changed from `Result<[u8; 32]>` to `Result<SecretScalar>`. The reconstructed key now lives inside a `ZeroizeOnDrop` wrapper from the moment it leaves the function — no window where the secret sits as a plain `[u8; 32]` on the caller's stack awaiting manual zeroization. Both call sites (`clsag_sign_multisig` in multisig.rs, the `msig-send` CLI handler in src/bin/wallet.rs) updated to drop their now-redundant `secret_bytes.zeroize()` calls. Internal intermediate `Scalar` values (signing shares, the working sum) still zeroized at function exit.
N/A src/wallet/persistence.rs:189-275   KDF params re-validated per unlock                       NOT-A-BUG (verified 2026-05-24): `validate()` is a few u32 range checks plus an RFC 9106 cross-constraint — cumulative cost is sub-microsecond. The actual unlock dominant cost is `derive_key` (Argon2id with m=256 MiB, t=3, p=4 — hundreds of ms by design). Caching `Params::new(...)` would shave a rounding-error fraction off something measured in seconds. Not worth the cache-invalidation surface.
N/A src/wallet/scanner.rs:568-622       Coinbase old vs new amount format guessed by addr match  NOT-A-BUG (verified 2026-05-24): line refs were stale; real code at scanner.rs:568-622. Format discrimination is wallet-side cosmetic only — both branches end up reading the same plaintext-LE-u64 amount from `output.encrypted_amount`. Worst-case adversarial output: passes old-format check (`stealth_address == spend_public`, which requires the attacker to know the recipient's public key) → wallet briefly considers a foreign output as theirs with whatever amount the encrypted_amount bytes decode to. Not spendable (no key image, no decrypt key) and harmless on balance display. Adding an OUTPUT_VERSION bit is a tx-format change (consensus surface) and would be appropriate at the next planned hard fork — out of scope for v1.0 audit polish.
DEFER-PERF src/wallet/scanner.rs:653-664  shared_secret computed twice per output                CONFIRMED REAL (verified 2026-05-24): `is_output_ours` (src/crypto/stealth.rs:932) computes `tx_point.mul(&view_scalar)` to derive the one-time scalar; on match, scanner.rs:655 then calls `compute_shared_secret` which redoes the same scalar-mul (scanner.rs:1090). 0.4% slow-path duplication (the 99.6% view-tag filter rejects most outputs before reaching either function). Fix is structural: change `is_output_ours -> bool` to return `Option<[u8; 32]>` (the shared_point bytes) so caller hashes once. Real but performance-only, not correctness; deferring until after the audit firm prioritizes — they care about correctness findings, not 0.4% scan-perf wins.
N/A src/wallet/lightsync.rs:535          compute_view_tag_light returns 0xFF on invalid pt        NOT-A-BUG (verified 2026-05-24): the 0xFF sentinel is a defensive default. View tags are 1-byte filtering hints — if pt deserialization fails (only possible with corrupt input), returning 0xFF causes the output to NOT match any wallet's expected_tag, so it's silently skipped during scan. Refactoring to Result would force every caller to handle the error path with no security improvement (corrupt outputs are rejected at block-validation time anyway).
N/A src/wallet/balance.rs:111           key_image_index stale entries never GC'd                  NOT-A-BUG (verified 2026-05-24): preserving the index entry across `mark_spent` is BY DESIGN per the doc comment at lines 196-199 — `unmark_spent_by_key_image` (used during reorg rewind for the Task #1b case where the only reason a UTXO was marked spent is a tx in a now-orphaned block) needs the entry to flip the spent flag back. The only call site that removes from `self.utxos` (`remove_outputs` at line 164) already removes the corresponding `key_image_index` entry on line 165. Index is structurally kept consistent — "stale" entries are intentional, not garbage.
✓  src/wallet/mnemonic.rs:78            for_each_word closure could leak refs                     ALREADY FIXED in HEAD (verified 2026-05-24): the function signature is `pub fn for_each_word<F: FnMut(&str)>(&self, mut f: F)` — `FnMut(&str)` already implies the closure returns `()`. A SECURITY comment block (lines ~70-78) explains the visitor-pattern design choice to avoid materializing a `Vec<&str>` that could escape. Stale entry.
DEFER-PERF src/wallet/send.rs:290-313  Pending UTXO maturity height recomputed                   ERROR-PATH ONLY (verified 2026-05-24): the `latest_pending_height` calculation runs only inside the `BalancePendingMaturity` error branch — i.e., only when the user attempted to send and got an insufficient-balance result with pending UTXOs. Not a hot loop. Caching would require invalidation on `add_utxo` + `mark_spent` for what amounts to one O(n) walk per user-initiated send attempt that failed. Not worth the cache-coherence complexity; deferring as low-priority perf opt.
L  src/wallet/history.rs:254-268        add() merges via O(n²) linear search                      HashSet for output_indices
L  src/wallet/wallet_keys.rs:90         master_seed sentinel [0xFFu8;32] non-standard             Use canonical zero
L  src/wallet/keys.rs:243-245           diversify_hash silently None on identity pt               Log + reject
L  src/wallet/background_sync.rs:372    record_error stores msg but never surfaces it             Expose via progress()
L  src/wallet/churn.rs:181              min_fee_reserve = 10_000 hardcoded                        Compute from MIN_FEE_PER_BYTE
L  src/wallet/subaddress.rs:159-161     MAX_ACCOUNTS=100, MAX_SUBADDRS=10k hardcoded             Doc + version-negotiate
L  src/wallet/wallet.rs:195-240         restore_from_mnemonic doesn't verify against expected     Add checksum verify
```

**Wallet verdict:** Multiple H items in [persistence.rs](c:\dev\coincync\src\wallet\persistence.rs) — the wallet-file format is the highest-leverage area to harden, because format bugs can become migration nightmares post-mainnet. Worth considering a `v4` wallet file format that authenticates the full header + addresses the partial-rename leak. **Verify the ScopedViewKey finding first** — exposing view_secret is the actual purpose of that type for selective disclosure (Article IX-compliant feature).

---

## Storage + DB — `src/storage/` + `src/db/` (13 findings)

```
✓  src/storage/shielded.rs:297-306     Checkpoint stack push only if tree accepted               ALREADY FIXED in HEAD (verified 2026-05-24): code calls `self.tree.write().checkpoint(height)` first; if it returns false, side-table push is skipped and a tracing::warn! fires explaining "two checkpoint stacks in sync". Exactly the fix the punchlist asked for. Stale.
✓  src/storage/spark.rs:191-199        Phase-2 checkpoint stacks may desync                       FIXED 2026-05-24: two single-file debug_assert! invariants added to checkpoint_at_height — (1) strict-monotonic height (catches out-of-order chain-orchestrator calls), (2) bounded-stack post-trim length. Cross-store invariant (spark/kernel/shielded len equality) belongs in chain.rs since spark.rs can't see the other stores; flagging that as a separate follow-up if the audit firm requests it.
✓  src/db/blocks.rs:157                unwrap_or([0;8]) on height key — corrupt = height 0       ALREADY FIXED in HEAD (verified 2026-05-24): src/db/blocks.rs:159-167 uses `try_into().map_err(|_| Error::DatabaseError(...))?` and propagates corruption as a Result instead of silently treating it as height 0. Punchlist entry was stale.
~~H  src/db/utxos.rs:131-152~~          **FALSE POSITIVE — STRUCK 2026-05-21.** There's no CAS loop — `compare_and_swap` is a single call (lines 116-122). Success → cleanup; failure → already-spent, return `Ok(false)`. Agent confabulated the loop.
✓  src/storage/shielded.rs:350-357     Rewind continues if tree refuses                           INTENTIONAL DESIGN (verified 2026-05-24): code at 350-365 logs the desync with tracing::warn! but DELIBERATELY aligns the side tables to the checkpoint boundary anyway — comment block explicitly explains "leaving them ahead of the tree is strictly worse". Original punchlist concern was "fail-hard or retry"; user's shipped decision (with reasoning in-code) is "log + force alignment because alternative is permanent divergence". Closing as not-a-bug.
N/A src/db/mempool.rs:121-152          Double-spend check pre-transaction → TOCTOU                NOT-REACHED (verified 2026-05-24): grep across src/ found ZERO non-test callers of `db::mempool::MempoolDb::add` — the production mempool path is `src/mempool.rs` (in-memory) which serializes admits via `&mut self`. The on-disk MempoolDb is currently a dormant persistence target. The TOCTOU is real *in principle* (two threads could pass the pre-check then both `insert` into key_images, upserting silently) but unreachable from the production mempool path. Audit-prep flag: BEFORE wiring MempoolDb as the live mempool, move the dup-check INTO the txn closure using `tx_trees[1].get(ki_key)?` with abort-on-conflict.
M  src/db/shim.rs:261-269              Single put_cf — not batched                                OK; document
✓  src/db/keys.rs:131-144,158-174     try_into silent failure on bad key                         ALREADY FIXED in HEAD (verified 2026-05-24): both `get_all_view_keys` (lines 137-143) and `get_current_epoch` (lines 162-168) use `try_into().map_err(|_| DatabaseError(format!(...)))?` and propagate corruption with explanatory messages. Doc comments at lines 127-130 and 154-157 explain why silent coercion to epoch 0 would be worse than failing the lookup loudly.
M  src/db/state.rs:163-169             Iterator error mid-collect → partial cleanup               Two-pass or retry
N/A src/storage/utxos.rs:204-240       Evict can race with output_index_iter                      NOT-A-BUG (verified 2026-05-24): `evict_old_outputs` takes `&mut self`, `output_index_iter` takes `&self` — Rust's borrow checker prevents both being held simultaneously by the same caller. The wrapping context is `Blockchain.inner: RwLock<BlockchainInner>` (src/chain.rs:306), and UtxoSet lives inside `BlockchainInner`. All mutating callsites of `evict_old_outputs` go through `inner.write()` (chain.rs:787, 1464), which structurally precludes any concurrent reader holding an iterator. The race the punchlist warned about is impossible without bypassing the RwLock.
M→DOC src/db/utxos.rs:364-383         Corrupt count silently overwritten with 1                  DOCUMENTED MITIGATION in HEAD (verified 2026-05-24): code at 358-363 has a comment block + emits `tracing::error!("DB CORRUPTION: height_counts[height={}] has {} bytes, expected 8 — treating as 0", ...)` so the operator sees the corruption. Code can't return `Err` from inside `sled::Tree::fetch_and_update`'s closure (the trait signature is `FnMut(Option<&[u8]>) -> Option<Vec<u8>>` — no Result). True propagation requires switching to a `Tree::transaction` + manual get/set pattern. Defensible defer: the loud tracing::error! gives operators the signal; a full repair-mode is a larger storage-layer change.
L  src/storage/shielded.rs:298-314     Oldest-checkpoint silently dropped at cap                  Add counter/log
L  src/db/mod.rs:325-413               apply_reorg "oldest-wins" pre-txn read                     Doc lock requirement
L  src/storage/kernels.rs:123-132      borsh encode .expect("infallible")                         Add round-trip test
L  src/storage/spark.rs:156-168        .expect on transient disk error → panic                    Distinguish transient/fatal
```

**Storage verdict:** Most H items are in the **dormant** Phase-2 stores ([shielded.rs](c:\dev\coincync\src\storage\shielded.rs), [spark.rs](c:\dev\coincync\src\storage\spark.rs)). These don't run in v1.0 production (Phase-2 ships in its own later release), so they're audit-prep for the v1.x activation, not v1.0 mainnet blockers. The `db/blocks.rs:157` height-parse and `db/utxos.rs:131` CAS loop ARE v1.0-critical and worth fixing now.

---

## Transaction + chain + mempool (10 findings)

```
~~H  src/mempool.rs:306-314~~           **FALSE POSITIVE — STRUCK 2026-05-21.** Ring size IS checked via `preflight_check` → `validate_transaction_basic` (validator.rs:70-72) BEFORE any crypto. No bypass path.
~~H  src/mempool.rs:479-506~~           **FALSE POSITIVE — STRUCK 2026-05-21.** Entire `admit_after_checks` runs under `&mut self` borrow with no async/await between conflict-find and remove. No race window — single-threaded admit.
N/A src/chain.rs:2502-2528             find_fork_point no parent-height invariant                 NOT-A-BUG (verified 2026-05-24): the walk uses a visited-set + main-chain-hash equality match (line 2546) to find the fork point. The cited "assert parent.height + 1 == current" check would be redundant — block storage already invariants on height-to-hash mapping via `get_block_hash(height)`. Adding the assert would only catch DB corruption that the visited-set + hash-match already rejects via the `None` return.
~~M  src/mempool.rs:138-148~~              **FALSE POSITIVE — STRUCK 2026-05-23.** Already mitigated by SECURITY (A5-MEM-01) clamp at line 145. The "silent" saturation requires fee > entire supply (6×10^15 atomic units total) to engage — unreachable in practice.
DEFER-TEST src/chain.rs:1415-1478      Race-detected block reaches fork-eval                      VERIFIED CORRECT in HEAD (2026-05-24): SECURITY (RACE-R7) re-check at lines 1418-1431 explicitly catches the race and lets the block fall through to fork-evaluation. Comment block at 1474-1477 spells out the intended fall-through behavior. The punchlist's ask ("test confirming no-reorg") requires a multi-threaded chain-mutation scenario which is hard to express deterministically in unit tests — would need a controlled-clock harness around `add_block` calls from two threads. Defensible to defer; the design itself is sound.
✓  src/chain.rs:2529-2528             Cycle detection returns silent 0 (genesis)                 ALREADY FIXED in HEAD (verified 2026-05-24): `find_fork_point` now returns `Option<u64>` — `None` for cycle-detected or parent-not-in-storage corruption (lines 2541, 2554-ish); `Some(0)` reserved for the legit "fork point IS genesis" case. The 2026-05-23 audit-prep fix (doc block at lines 2517-2528) explicitly separated corruption from genesis. Caller `add_block` MUST treat `None` as corruption-class rejection per the doc.
L  src/transaction/types.rs:54-69      Fallback hash misses tx_type                               Add tx_type byte
L  src/mempool.rs:104-109              unix_now() unwrap_or(0) on clock fail                      Fail-closed or last-recorded
L  src/mempool.rs:495-496              to_replace dedup is O(n) Vec scan                          HashSet<Hash>
L  src/chain.rs:2469-2498              fork-work walk has no depth bound                          break past reasonable depth
```

**Transaction/chain/mempool verdict:** The two H items both need source verification — `validate_transaction_basic` may already enforce ring size before crypto, in which case the "bypass" claim is a false positive. RBF race is more likely real. The fee_per_byte clamping is real and consensus-relevant.

---

## Top-level src + cli + bin + primitives + emission (5 real findings, lots of green)

```
✓  src/bin/node.rs:128                       Tracing init panic on bad RUST_LOG                  ALREADY FIXED in HEAD (verified 2026-05-24): uses `try_from_default_env().or_else(parse cli.log_level).unwrap_or_else(eprintln + fallback to "info")` at lines 128-141. Bad RUST_LOG produces a stderr warning and falls back to "info". Punchlist entry was stale.
M  src/bin/update_critical_hashes.rs:70      Build-tool panic on FS error                        DOWNGRADED H→M (verified 2026-05-24): `unwrap_or_else(|e| panic!("read {}: {}", file_path, e))` does panic on FS error, BUT this is a BUILD TOOL — runs during CI/release setup, not user runtime. Panic = loud CI failure with the file path, which is the right behavior for a setup-time tool. Wrapping in Result would let CI keep going with corrupt hashes, which is strictly worse. Not blocking.
~~M  src/config.rs:569~~                       **FALSE POSITIVE — STRUCK 2026-05-23.** P2P bind 0.0.0.0 is correct — a P2P node needs inbound to seed/relay the network. Bitcoin's bitcoind + Monero's monerod both default to 0.0.0.0 for P2P; only the rig metrics endpoint should be 127.0.0.1-only.
~~M  src/error.rs:337~~                       **DONE 2026-05-23.** Converted `IoError(#[from] std::io::Error)` → struct variant `IoError { kind, source }`. Display now shows only `ErrorKind` (e.g. `IO error: NotFound`), not the path-bearing `std::io::Error` message. `?` operator preserved via manual `From<std::io::Error>`. 17/17 wallet::persistence tests pass.
L  src/helpers.rs:99                         RateLimiter TODO: busy-wait                         Replace with governor crate
```

**Verdict:** Mostly green. Constants, mainnet/testnet feature gates, supply curve math, and primitive types all looked correct under scrutiny. The `RUST_LOG` panic is real and easy to fix. The `update_critical_hashes` panic is build-time only — low priority.

---

## FROST coord + rolling-finality (2 real findings, 16 verified-correct)

```
~~H  crates/coincync-frost-coordinator/src/bin/coord.rs:736-737~~    **DONE 2026-05-23.** Changed `let secret = coord.invite_secret;` (copy) to a direct borrow `verify_token(&coord.invite_secret, ...)`. Removes the stack-resident secret copy. Workspace check clean.
N/A crates/coincync-frost-coordinator/src/bin/coord.rs:717        Rate-limit trusts peer.ip()             NOT-A-BUG (verified 2026-05-24): the coordinator is a raw WebSocket server that NEVER reads X-Forwarded-For — `peer.ip()` is the actual TCP socket's peer address from the kernel (line 524, plumbed from `handle_connection(_, _, peer: SocketAddr)`). There is no header-trust surface to exploit. The fix the punchlist suggested ("ensure reverse-proxy strips XFF") doesn't apply because we don't *read* XFF at all. Conservative-by-design: if deployed behind a reverse proxy, all clients share one bucket per proxy IP (rate-limits harder), which is strictly safer than the alternative of trusting a spoofable header.
M  crates/coincync-frost-coordinator/src/bin/coord.rs:322        Single invite_secret across sessions    Per-session secrets (planned Phase 6)
```

**Verdict:** Both crates came back overwhelmingly clean. Constant-time HMAC, idempotent attach, non-idempotent message declaration, strict borsh parsing, ed25519 fail-fast, integer-only Byzantine threshold, correct active-set windowing — all already done. This is the cleanest subsystem in the codebase.

---

## Severity rollup

| Subsystem | H | M | L | Total | Notes |
|---|---|---|---|---|---|
| Network | 2 | 6 | 6 | 14 | One real DoS (Noise frame size), rest polish |
| Consensus | 5* | 8 | 5 | 18 | *4 of the 5 H need source verification |
| Crypto | 2 | 4 | 5 | 11 | Mostly doc gaps; `lelantus_spark thread_rng` needs verify |
| Mining + rig | 4 | 7 | 5 | 16 | Two real operator-posture H items |
| RPC | 2 | 4 | 4 | 10 | Both H are explorer WebSocket DoS |
| Wallet | 5 | 9 | 7 | 21 | Persistence layer is the leverage point |
| Storage + DB | 5 | 6 | 4 | 15 | 3 of 5 H are in dormant Phase-2 stores (v1.x) |
| Transaction/chain/mempool | 2 | 4 | 4 | 10 | RBF race + fee-overflow merit verification |
| Top-level/cli/bin | 2 | 2 | 1 | 5 | Mostly green |
| FROST + rolling-finality | 1 | 2 | 0 | 3 | Cleanest subsystem |
| **Totals** | **30** | **52** | **41** | **123** | |

(Counts are post-de-duplication; raw agent findings totaled ~155 before consolidation.)

---

## Verification protocol before fixing

Per the project standing rule: **subagent findings have a ~50% false-positive rate on file:line specifics. Read the cited line in source before opening a fix PR.** Pattern:

1. Pick an item from the punch list
2. Open the file at the cited line
3. Confirm the issue is real, not a misread of the surrounding code
4. If real: open a focused fix PR (one item per PR is fine; keep them reviewable)
5. If false positive: strike it from this doc with a one-line note explaining why

This doc lives at `docs/v1.0-base-chain-hardening-punchlist.md` — update it in place as items are verified, fixed, or struck.

---

## Recommended next session

Work the **Top 10** in order. Most are 15-30 minute fixes. Each one closed is one less audit finding written up. By the time the base-chain audit firm receives the commit, the top of this list should be empty and the rest annotated.

Items deliberately deferred:
- Phase-2 store findings (`src/storage/shielded.rs`, `src/storage/spark.rs`) → these are dormant in v1.0; fix during the Phase-2 activation release prep
- Multi-account OVK binding (`src/wallet/keys.rs:79`) → no multi-account UX in v1.0
- Wallet file v4 format (`src/wallet/persistence.rs:501`) → real concern, but a format migration is itself audit-relevant; defer the migration design until the audit firm weighs in
- `lelantus_spark thread_rng` → verify whether it's consensus-reachable; if no, document and skip

Items deliberately kept:
- Everything in the Top 10
- Mining/rig posture (metrics bind, Stratum TLS) — these become Discord-visible operator complaints if shipped as-is
- FROST `invite_secret` stack zeroization — small, easy, audit-friendly
- RBF race in mempool — verification first, then a tight fix
