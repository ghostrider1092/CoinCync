{
  "name": "coincync-wallet",
  "version": "1.0.0",
  "description": "CoinCync Privacy Wallet — Private by law. Private by math.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "bundle:sidecars": "node scripts/bundle-sidecars.js",
    "build:chain": "cargo build --release --features randomx,testnet --manifest-path ../Cargo.toml --bin coincync-node --bin coincync-wallet --bin coincync-tui-miner",
    "build:release": "npm run build:chain && npm run pack",
    "tauri": "tauri",
    "start": "tauri dev",
    "pack": "npm run tauri build"
  },
  "dependencies": {
    "globe.gl": "^2.45.3",
    "qrcode": "^1.5.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@tauri-apps/api": "^1.5.0",
    "@tauri-apps/cli": "^1.5.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}
