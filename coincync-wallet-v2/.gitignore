# Build outputs
/src-tauri/target/
# NOTE: src-tauri/Cargo.lock is intentionally COMMITTED (it is a binary app,
# not a library) for reproducible builds — do not re-ignore it.

# Tauri v2 generated schemas/permissions — regenerated on every build.
/src-tauri/gen/

# Dependencies (will be installed by npm install)
/node_modules/

# Editor / OS
.DS_Store
*.swp

# Dev artifacts
/dist/
